# Godot AI

Godot Editorの操作、状態確認、実行時の確認にはGodot AIを適宜使用する。

## 使用する場合

- Godot Editorの状態確認や、Scene・Node・ResourceなどEditor上の操作には優先して使用する。

## 使用しない場合

- GDScriptとShaderの作成・編集は、直接編集を優先する。
- 文字列・パスの一括置換など、複数ファイルにまたがりEditorを必要としない処理は、直接編集を優先する。
